{
  "name": "mbti2-h5",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 8765",
    "build": "vite build && mkdir -p dist/assets && cp -R assets/. dist/assets/ && cp -R uploads dist/uploads && find dist -name .DS_Store -delete",
    "preview": "vite preview --host 127.0.0.1 --port 8765"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
